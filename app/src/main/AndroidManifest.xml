<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bmicalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BMICALCULATOR">
        <activity
            android:name=".Obese"
            android:label="Obese Category"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".OverWeight"
            android:label="OverWeight Category"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Normal"
            android:label="Normal Category"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Underweigt"
            android:label="UnderWeight Category"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".quick_check"
            android:label="Chart Box"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>